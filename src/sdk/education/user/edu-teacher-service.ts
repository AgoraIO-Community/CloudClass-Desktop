import { EduUserService } from './edu-user-service';
import { EduCourseState, EduShareScreenConfig, EduStream, EduUser } from '../interfaces';
import { AgoraEduApi } from '../core/services/edu-api';
export class EduTeacherService extends EduUserService {
  // constructor(apiService: AgoraEduApi, user: EduUser) {
    // super(apiService);
    // this.updateLocalUser(user)
  // }
}
import React from 'react'

export interface DeviceDialogProps {
  title: string
  component: React.ReactElement
}

export const DeviceDialog: React.FC<DeviceDialogProps> = (props) => {
  return (
    <div></div>
  )
}